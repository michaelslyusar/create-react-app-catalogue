// Get visible results
const getVisibleResults = (results, { filterText }) => {
    return results.filter((result) => {
        result
    }).sort((a,b) => {
        return 1;
    })
};


export default getVisibleResults;